[project]
name = "passforge"
version = "1.0.0"
description = "Cryptographically secure password generator"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "pystray>=0.19",
    "pillow>=10.0",
]

[dependency-groups]
dev = []
